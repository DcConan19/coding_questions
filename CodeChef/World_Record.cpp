#include<bits/stdc++.h>
using namespace std;
#define ll long long int

void solve()
{
    float k1,bruh,k2,k3,v,can;
    can = (k1*k2*k3*v);
    can = (float)100/can;
    // bruh = (int)(can * 100 + .5);
    // float(bruh)/100;
    if (can > 9.58){
        cout<<can<<endl;
    }
    else {
        cout<<can<<endl;
    }
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll t = 1 ;
    cin >> t;
    ll x = 1;
    for (int i = 1; i <= t; i++)
    {
        solve();
    }
    return 0;
}